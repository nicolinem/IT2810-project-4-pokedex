type Pokemon {
  total: Int
  type2: String!
  type1: String!
  name: String!
  pokemonID: Int
  HP: Int
}

type Type {
  name: String!
}

type Query {
  getPokemon: [Pokemon]
    @cypher(statement: "MATCH (g:Pokemon) RETURN g ORDER BY g.name ASC")
}
